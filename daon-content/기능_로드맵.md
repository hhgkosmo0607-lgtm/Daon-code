# 기능 로드맵 (2026-09-18 정리)

`추가추가.md`에 적어둔 아이디어들을 코드베이스 현황과 대조해서 정리한 내용.
우선순위 순서대로 나열.

## 1. 오답노트 화면 (다시풀기 + 오답 이유 설명 통합) — 완료
- `wrong_answers` 테이블을 읽어 보여주는 전체 오답노트 화면 (`/review`,
  `WrongAnswersScreen` + `useWrongAnswers`) — 문제/정답/explanation을 보여주고
  맞히면 그 자리에서 오답노트에서 빠짐. 홈 상단바에 오답 개수 배지 추가
- 레슨을 끝낸 직후 "틀린 문제 N개 바로 풀기" 버튼 추가 (듀오링고식 즉시 복습) —
  방금 세션에서 이미 알고 있는 틀린 문제를 DB 재조회 없이 바로 큐로 넘겨서 재사용
  (`LessonScreen.tsx` → `WrongAnswersScreen seedQuestions={...}`)
- 참고: DB에 `user_term_review`(라이트너 박스 간격 반복) 테이블도 이미 있는데 코드에서
  전혀 안 쓰이고 있음. 나중에 "며칠 뒤 다시 보여주기" 기능 붙일 때 활용 가능

## 2. 코드 문제 언어 라벨 — 완료
- 예상보다 범위가 작았음: 콘텐츠 169개 코드 문제를 스캔해보니 전부 JS 아니면 JSX라서
  (`codeHighlight.ts`에 이미 "이 앱 코드는 전부 JS/JSX"라는 전제가 있었음), JSON
  파일에 값을 채우는 대신 `detectLanguageLabel()`로 코드 내용에서 그때그때 추론
  (`shared/components/codeHighlight.ts`, `CodeBlock.tsx`)
- 발견: `code` 필드가 실제 코드 말고 에러 메시지·터미널 출력(`git`/`npm`)·빈칸 채우기
  문장에도 재사용되고 있었음 (169개 중 74개, 거의 절반) — 그런 경우는 라벨을 아예 숨김
- 테스트 범위가 `features/**/domain`으로 제한돼 있어서 `vitest.config.mts`를
  `shared/**`까지 넓히고 `codeHighlight.test.ts` 추가

## 3. 문제 품질 수정 (상식 문제, 애매모호한 정답) — 완료
- 서브에이전트로 전체 401문제(58개 파일) 감사 → 명확한 문제 9개 발견, 8개 수정
  - `9-2-q4`: "navigate는 expo-router에 없다"는 틀린 설명 제거 (실제로 존재함)
  - `3-1-q5`: "가장 흔한 원인은?" → "가장 먼저 확인해봐야 할 것은?"으로 재구성 (다른 보기도
    배제 못 하던 문제)
  - `1-3-q1` / `1-2-q1` / `5-1-q1`: 정답·오답 보기가 사실상 같은 말이던 문제 — "흔해서
    골랐다"가 아니라 "정보가 없어서 어쩔 수 없이 채운 것"이라는 인과관계를 설명에 명시
  - `6-5-q5`: "같은 브랜치인지"를 지문에 명시해서 다른 보기도 맞을 수 있던 상황을 배제
  - `4-1-q6`: "npm run dev가 설치 중일 수 있다"는 틀린 설명을 "번들러 캐시 생성 중"으로 정정
  - `12-4-q4`: "React가 use를 인식한다"는 과장된 설명을 "관례 + eslint 검사"로 정정
- 나머지 3개(`11-3-q6` nit: 의미, `4-4-q4` F12 단축키, `4-4-q6` 콘솔 빨간 숫자)는 암기성
  문제이지만 초보자에게 실용적으로 유용한 지식이라 그대로 두기로 결정

## 4. 자격증 트랙 추가 (SQLD / 정보처리기능사 / 정보처리산업기사 / 정보처리기사) — 구조는 완료, 콘텐츠는 대기
- 결정: 기존 "코딩 입문 + AI 활용" 커리큘럼은 그대로 두고, 자격증 대비를 **병행 트랙**으로 추가
- 완료된 구조 작업:
  - `Lesson`/`Stage`에 `trackId` 필드 추가, 새 `Track` 타입 도입 (`features/lesson/domain/types.ts`)
  - `content/tracks.json` 신규 — 트랙 레지스트리 (`ai-coding`, `ipe`)
  - `content/lessons.json`의 기존 12개 스테이지·58개 레슨 전부 `trackId: "ai-coding"`로 표시
  - `정보처리기사` 파일럿 스테이지/레슨 1개(`ipe-1-1`, 문제 콘텐츠 없음 — "준비중"으로 뜸) 추가
  - `contentRepository.ts`의 `getStages`/`getLessons`가 trackId로 필터링 (기본값 `ai-coding`이라
    기존 화면은 그대로 동작). `getLesson`은 전역 id로 트랙 무관하게 찾음(진행도 테이블이
    lesson_id 하나로만 구분하기 때문)
  - `features/track/TrackContext.tsx` — 테마 선택과 같은 패턴(AsyncStorage에 기기별로 저장,
    계정에는 안 묶음). `/settings/track` 화면에서 전환, 홈 화면 상단에 현재 트랙 표시줄 추가
  - Supabase Edge Function 쪽(`_shared/content.ts`)은 안 건드림 — lesson_id 조회는 트랙과
    무관하고, 배치고사 로직은 ai-coding 레슨만 다뤄서 그대로도 안전함
- 남은 일: 정보처리기사 실제 문제 콘텐츠 — 사용자가 기준 자료(기출/시험범위)를 주면
  그걸 바탕으로 같이 작성 (임의로 시험 범위를 판단해서 채우지 않기로 함)

### SQLD 트랙 — 완료
- 정보처리기사보다 먼저 착수 (시험 범위가 좁고 공개돼 있어서)
- 참고 자료: yunamom.tistory.com (SQLD 기출복원/이론 블로그) — 기출복원 글은 인터랙티브
  퀴즈 위젯이라 텍스트 추출 불가, 이론 글(PIVOT/UNPIVOT 등)로 출제 스타일만 참고함.
  오라클 문법 세부사항(서브쿼리 별칭, 컬럼 인용 규칙 등)까지 파고드는 문제가 있어서,
  SQL 세부 챕터는 실제 기출/수험서 없이 임의로 쓰지 않기로 함
- 트랙 구조는 공식 커리큘럼 그대로 세팅 (`content/tracks.json`에 `sqld` 추가):
  - 과목 I: 1장 데이터 모델링의 이해(엔터티/속성/관계/식별자), 2장 데이터 모델과 SQL(정규화/조인/NULL/트랜잭션)
  - 과목 II: 1장 SQL 기본(SELECT/함수/WHERE/GROUP BY/JOIN/서브쿼리), 2장 SQL 활용(집합연산자/계층형질의/PIVOT/윈도우함수/정규표현식), 3장 관리구문(DDL/DML/TCL/DCL)
  - 총 5스테이지 22레슨 뼈대(`content/lessons.json`)를 다 만들어뒀고, 문제 콘텐츠는 아직
    파일럿 1개(`sqld-1-1`)만 있음 — 나머지는 "준비중"으로 뜸
- **전체 22레슨(84문제) 작성 완료** — 사용자가 알려준 SQLD 참고 블로그(yunamom.tistory.com)의
  "SQLD 이론" 카테고리(과목II 1~3장 정리글, PIVOT/UNPIVOT, 정규표현식, 부정비교연산자 글)를
  근거로 오라클 문법 세부사항(CONNECT BY PRIOR 방향, MINUS는 Oracle 전용, NVL vs ISNULL,
  DELETE/TRUNCATE 롤백 가능 여부 등)까지 확인 후 원문을 베끼지 않고 원본 문제로 작성함.
  기출복원 글 자체는 인터랙티브 위젯이라 텍스트 추출이 안 돼서 실제 기출 그대로 옮긴 건 없음
- 구조 검증 스크립트로 전체 84문제의 id 중복, answer 인덱스 범위, order 타입 정답 배열이
  options의 순열인지까지 확인함
- **사용자 검토 필요** — 특히 Oracle 문법 세부사항(3~5단계) 문제들은 실제 오라클 환경이나
  최신 기출과 대조해서 확인해주면 좋음. 틀린 부분 있으면 바로 수정

## 5. 캐릭터 / 펫 기능
- 코드베이스에 관련 흔적 전혀 없음, 완전 신규
- 에셋, 상태 저장, 화면, 상호작용 로직까지 필요해 스코프가 큼
- 학습 효과보다 리텐션/재미 요소라 우선순위 가장 낮게 둠
- (참고: 이 세션에 연결된 "openpets" MCP 도구는 사용자 데스크톱의 별개 프로그램이고
  이 앱과는 무관함 — 혼동 주의)

## 6. 언어별 기초 문제 트랙 — 자바·파이썬 완료, 이후 언어는 대기

사용자가 실제로 공부했던 교안(`D:\02Workspaces_backup`)을 기준으로 언어별 "기초지식"
트랙을 만드는 작업. 이 폴더 안에 02Oracle(SQLD로 이미 다룸), 03Java, 04Python,
05Visualization, 06Flask, 07HTML, 08AI, 09CSS 순으로 교안이 있어서, 순서대로 진행 중.

### 문제 품질 관련 교훈 (자바 트랙에서 발견, 파이썬부터 반영)
- 자바 트랙 작성 초기에 정답을 항상 1번에 두고 쓰는 습관이 있어서 233문제 중
  97.9%가 정답 1번에 몰렸던 걸 나중에 발견해서 스크립트로 전부 재배치함
  (자세한 내용은 아래 "문제 오답 품질 점검" 참고)
- 파이썬 트랙 작성 때도 습관대로 정답을 먼저 쓰게 돼서(83문제 중 82개가 답 1번),
  매 트랙 작성 끝에 답 위치를 강제로 섞는 스크립트를 항상 돌리기로 함 — 글 쓰는
  습관 자체를 못 고치니 기계적으로 검증하는 단계를 프로세스에 넣음

### 자바 기초 트랙 — 완료
- `content/tracks.json`에 `java` 트랙 추가. 교안(`03Java/JavaStudy/src/ex01~ex17`) 폴더
  구조를 그대로 20개 스테이지로 매핑 (ex01 자바 시작하기 ~ ex17 컬렉션/제네릭)
- 사용자 요청으로 "신입 개발자 소양 플러스알파" 3개 스테이지 추가:
  자료구조/알고리즘 기초, 객체지향 설계 원칙(SOLID 5개), 자바 실무 컨벤션/도구
  (`ex20io`, `ex21jdbc`, `skillup` 프로젝트 폴더는 "기초"보다 응용이라 제외)
- 총 20스테이지 70레슨 153문제 작성 완료, 구조 검증(id 중복/answer 인덱스/order
  순열) 통과
- **구현 중 발견한 실제 버그 2건도 같이 고침** (Java 코드를 실제로 렌더링해보다 발견):
  1. `detectLanguageLabel`이 JS/JSX만 구분해서, 자바 제네릭 `List<String>`을 JSX
     태그로 오인함 → `Question.language` 필드를 신설해서 명시적으로 언어를 지정할
     수 있게 하고(안 쓰면 기존처럼 자동 추론), 태그 색칠 자체도 `skipTags` 옵션으로
     끌 수 있게 함 (`shared/components/codeHighlight.ts`, `CodeBlock.tsx`)
  2. 문법 강조기가 식별자를 한 글자씩 훑다가 "main"의 뒷부분 "in"처럼 식별자 중간이
     우연히 키워드와 같아지는 지점에서 오매칭하는 기존 버그 발견 — 식별자를 통째로
     소비하는 fallback 패턴을 추가해서 해결 (JS 콘텐츠에도 잠재해있던 버그라 회귀 아님)
- **사용자 검토 필요 없음** — SQLD와 달리 자바 문법은 특정 DBMS 사투리 문제가 없어서
  일반 자바 지식만으로 정확도 확인 가능. 다만 실제로 앱에서 몇 개 풀어보고 난이도/설명
  퀄리티 피드백 주면 좋음

### 파이썬 기초 트랙 — 완료
- `content/tracks.json`에 `python` 트랙 추가. 교안(`04Python/01~19*.py`, 20번대 DB/크롤링/
  엑셀은 "응용"이라 제외) 파일 구조를 20개 스테이지로 매핑 (변수 ~ 예외처리)
- 자바와 마찬가지로 "파이썬다운 코드 스타일" 신입 소양 스테이지 추가:
  PEP8/네이밍, 리스트 컴프리헨션, 가상환경(venv)/pip, enumerate·zip 같은 관용구
- 총 20스테이지 38레슨 83문제 작성. 교안 자체가 자바 수업 다음에 이어지는 내용이라
  "자바와 이 부분이 다르다"는 비교 설명을 explanation에 적극 활용함(예: 들여쓰기 블록,
  동적 타이핑, self vs this, new 없는 인스턴스 생성 등)
- 정답 위치는 처음부터 스크립트로 재배치(83문제 중 82개가 또 답 1번으로 몰렸음 — 위
  "문제 품질 관련 교훈" 참고), 구조 검증(id 중복/answer 인덱스) 통과
- **사용자 검토 필요 없음** — 파이썬도 자바처럼 방언 문제가 없는 단일 표준 문법이라
  일반 지식만으로 정확도 확인 가능
### HTML 기초 트랙 — 완료
- `content/tracks.json`에 `html` 트랙 추가. 교안(`07HTML/H00~H19*.html`) 파일 구조를
  실제 소스 분량에 맞춰 9스테이지 14레슨으로 매핑 (문서 기본구조 ~ 웹 표준/접근성)
- "신입 소양" 스테이지로 웹 표준과 접근성(alt, aria-label, 시맨틱과 SEO) 추가
- 총 14레슨 32문제 작성, 처음부터 정답 위치 스크립트로 재배치(32문제 중 32개가
  또 1번으로 몰렸음 — 완전히 습관화된 패턴이라 매번 스크립트 필수)
- **05Visualization(pandas/matplotlib)·06Flask·08AI(numpy/머신러닝)는 보류** —
  언어 문법이 아니라 특정 라이브러리 API 사용법이라 지금까지의 "기초 문법 20스테이지"
  틀이 잘 안 맞음. 대안 아이디어를 정리해둠(사용자와 논의):
  - Flask: 프레임워크 문법보다 HTTP 요청-응답/라우팅 같은 공통 개념 위주로 일반화,
    또는 코드 실행결과 예측형·디버깅 시나리오형 문제로 전환
  - AI: 모델 구현 세부사항(수식 등) 대신 지도/비지도학습 차이 같은 개념 수준으로 스코프
    축소, numpy만 따로 떼어 "데이터 다루기" 트랙으로, 또는 파이프라인 순서 맞추기(order
    타입)로 정확도 리스크를 줄이는 방향
### CSS 기초 트랙 — 완료
- `content/tracks.json`에 `css` 트랙 추가. 교안(`09CSS/C01~C24*.html`, 40여개 파일)을
  12스테이지 21레슨으로 매핑 (선택자 ~ CSS 실무 컨벤션)
- "신입 소양" 스테이지로 CSS 실무 컨벤션(CSS 변수/커스텀 속성, BEM 네이밍과 명시도
  관리) 추가 — 교안 자체엔 없지만 실무에서 자주 쓰이는 최신 관례
- 총 21레슨 46문제 작성, 처음부터 정답 위치 스크립트로 재배치(46문제 중 34개가 또
  1번으로 몰림)
- **사용자 검토 필요 없음** — CSS도 브라우저 표준 문법이라 방언 문제 없이 일반
  지식만으로 정확도 확인 가능

### Flask 웹 개발 기초 트랙 — 완료 (개념 중심)
- `content/tracks.json`에 `flask` 트랙 추가. 문법 나열 대신 "웹 요청-응답 → Flask 시작 →
  템플릿/정적 파일 → 폼/요청 처리 → 세션/리다이렉트 → 파일 업로드 → 웹 보안 소양"의
  개념 흐름으로 7스테이지 14레슨 구성 (교안 `06Flask` 코드를 근거로 하되 개념 위주)
- 신입 소양: request.args vs form, 서명 쿠키 세션(secret_key), 디버그 모드 운영 금지,
  파일명 uuid 처리, XSS/escape, 비밀번호 해시

### AI 기초: 학습부터 직접 만들기 트랙 — 완료 (개념 중심)
- `content/tracks.json`에 `ai` 트랙 추가. 7스테이지 20레슨: AI/ML 개념 → 학습 데이터 →
  학습과 평가(훈련/테스트 분리, 과적합) → numpy → 데이터 다루기·시각화(05Visualization의
  pandas/matplotlib을 여기로 통합) → 나만의 모델 만들기(파이프라인 order 문제, joblib 저장,
  Flask 서비스 연결) → AI 활용 소양(데이터 편향, 할루시네이션, 개인정보, 저작권)
- 정확도 리스크를 줄이려고 수식/구현 세부 대신 개념·순서·주의점 위주로 출제

- flask+ai 합계 34레슨 76문제, 정답 위치 스크립트로 재배치 완료(0~3번 고르게 분포),
  구조 검증/tsc/eslint/vitest/번들 확인 통과

### 자바스크립트 기초 트랙 — 완료
- `content/tracks.json`에 `js` 트랙 추가. 교안(`10JavaScript`: 01Basic~08Form, J21~J29)을
  15스테이지 34레슨 108문제로 매핑 (변수/연산자/제어문/함수/배열/고차함수/문자열·Date·Math/
  객체·JSON/DOM/이벤트/폼/쿠키·스토리지/비동기(Promise·async·fetch))
- 신입 소양 스테이지: 옵셔널 체이닝·널 병합, 참조/얕은 복사, XSS·이벤트 위임·토큰 저장 위험,
  0.1+0.2·sort 기본 정렬·parseInt 같은 흔한 함정
- 이번엔 작성 스크립트가 정답 위치를 처음부터 순환 배치(0~3번 균등) — 별도 셔플 단계 불필요

### 리액트 기초 트랙 — 완료
- `content/tracks.json`에 `react` 트랙 추가. 교안(`11React`: react01~06, react10~11, project02~04)을
  12스테이지 25레슨 82문제로 매핑 (JSX/컴포넌트/props/이벤트·리스트/state/폼/라우터/useEffect/
  useRef·useMemo·useCallback/useReducer·Context·zustand/React 19 훅/Firebase 연동)
- 신입 소양 스테이지: 렌더링 중 setState 무한 루프, 훅 규칙, 의존성 누락(stale), 상태 끌어올리기,
  파생 값은 state로 두지 않기, dangerouslySetInnerHTML·javascript: URL, SPA 새로고침 404(rewrite)
- 코드 스니펫은 `language`를 비워 두고 CodeBlock의 JS/JSX 자동 추론에 맡김

### 스프링 부트 기초 트랙 — 완료
- `content/tracks.json`에 `spring` 트랙 추가. 교안(`12Springboot`: ex01~ex23) 순서를 13스테이지
  28레슨 85문제로 매핑 (MVC/DI/Thymeleaf/JdbcTemplate/MyBatis/트랜잭션/시큐리티/파일업로드/
  REST/JPA/메일). 코드 라벨은 Java·XML·HTML·Properties를 문제별로 지정
- 신입 소양: 계층 구조와 DTO 분리, 검증/전역 예외 처리, N+1, 로그·오류 노출, SQL 인젝션/XSS/
  소유자 권한 검사, 비밀 값 관리(교안은 csrf disable·설정 값 평문이라 실무 주의점을 설명에 명시)
- **오답 길이 편중 개선 — 완료**: 새 트랙 작성 후 통계를 내 보니 객관식에서 "가장 긴 보기가
  정답"인 비율이 java/python/html/css/sqld/flask/ai/react/spring 전부 70~94%(무작위 기대값 25%)
  였음 — 길이만 보고 맞힐 수 있는 상태. 8개 서브에이전트가 트랙별로 오답 보기만 정답과 비슷한
  길이·구체성으로 다시 씀(정답 위치·id·프롬프트·코드·설명은 그대로, 정답 쪽 장황한 표현은 다듬음)
  - 결과: 정답이 가장 긴 비율 java 32%, python 27%, html 38%, css 13%, sqld 26%, flask 22%,
    ai 12%, react 32%, spring 33% (반대로 "정답이 가장 짧다"가 몰리지 않는 것도 확인: 13~37%)
  - `git diff`로 242파일 620문제 전수 대조: 옵션 텍스트 외 변경 없음을 확인
  - 검수 중 발견해 직접 고친 것: react-12-3-q2(React 19는 href의 javascript: URL을 기본
    차단하므로 문구 정정), sqld-5-1-q1(TRUNCATE 롤백 불가는 오라클 기준임을 명시),
    sqld-3-2-q2/2-3-q1/1-3-q1(DBMS나 관점에 따라 정답으로 볼 여지가 있던 오답 보기 교체)
  - 앞으로 새 트랙을 쓸 때는 정답 위치 셔플과 함께 이 길이 통계도 확인할 것
    (스크래치 스크립트 `len-check.js`: 트랙 접두어별 정답 최장 비율 출력)

### 도커 기초 트랙 — 완료
- `content/tracks.json`에 `docker` 트랙 추가. 교안(`C:\02Workspaces\13Docker`: 06copy~12pythonDev와
  07커맨드.md/08커맨드.md 요약)을 9스테이지 19레슨 66문제로 매핑 (이미지/컨테이너, run·ps·exec·cp,
  볼륨/바인드 마운트, Dockerfile·build·레이어, Compose, DB·Tomcat·Flask 컨테이너)
- `14AWS` 폴더는 교안이 없어(Sample.txt 한 줄과 SSH 키 파일뿐) 별도 트랙 대신 "서버에 배포하기"
  1레슨(SSH 키 관리, 보안 그룹, push/pull 배포 흐름)만 일반 지식으로 흡수. 키 파일은 열어보지 않음
- 신입 소양: latest 태그 고정, .dockerignore, 경량 이미지, 비밀 값 평문 커밋 금지·이미지에 굽지 않기,
  root 실행 위험, DB 포트 공개 위험, prune 주의, 컨테이너 일회용 설계, depends_on≠준비 완료
- 코드 라벨 Bash/Dockerfile/YAML/Python 지정. 처음 쓴 상태는 정답 최장 83%였고 서브에이전트로
  오답을 다듬어 24%로 낮춤(정답 위치 17/17/16/16). **새 트랙을 쓸 때 처음부터 오답을 정답과
  같은 길이·구체성으로 써야 재작업이 없음**
- 남은 것: 정보처리기사(ipe) 실제 콘텐츠, 01선행학습 폴더·15JSP 미확인(15JSP는 아직 진행 중인 수업)

## 8. 국가기술자격 트랙 (추가추가.md 18~21줄) — 프로그래밍기능사 완료, 나머지 대기

사용자가 `추가추가.md`에 적어둔 4개 자격증(프로그래밍기능사, 정보기기운용기능사,
사무자동화산업기사, 정보처리기사) 중 "쉬운거부터" 진행하기로 함.
난이도/합격률 검색 결과 "정보기기운용기능사 ≈ 프로그래밍기능사 < 사무자동화산업기사"였지만,
프로그래밍기능사는 기존 자바·파이썬·SQL·HTML·CSS 트랙과 내용이 크게 겹쳐서 가장 먼저 착수.

### 프로그래밍기능사 대비 트랙 — 완료
- Q-net 공식 출제기준 전문 대신 웹 검색(나무위키 등)으로 확인한 정보를 근거로 구성:
  2026년 개정 필기 4과목(프로그래밍 언어/응용SW기초기술/SQL 활용/정보시스템 기초기술,
  객관식 60문항)이며 최근 회차는 Python·Java 실행결과 예측, HTML/CSS 명령어 서술 문제가
  많다는 경향까지 확인함. 다만 각 과목의 공식 세부 출제 항목(Q-net PDF)은 못 구해서,
  기존 트랙과 안 겹치는 일반 CS 기초 위주로 스코프를 잡기로 사용자와 합의
  (AskUserQuestion으로 확인 후 진행 — "정보기기운용기능사/사무자동화산업기사"부터
  시작 대신 "지금 조사한 내용으로 바로 시작"을 선택함)
- `content/tracks.json`에 `pgcert` 트랙 추가. 9스테이지 18레슨 72문제:
  SW공학 기초, 운영체제 기초, 컴퓨터구조 기초, 자료구조/알고리즘 기초, 네트워크 기초,
  정보보안 기초, **C 언어 기초(신규 — 이 앱에서 처음 다루는 언어)**, 실행결과 예측
  집중훈련(C/자바/파이썬/HTML·CSS 혼합), SQL 활용 핵심 요약(깊은 내용은 SQLD 트랙 안내)
- 코드가 있는 문제(특히 C 포인터, 파이썬 연산자 우선순위)는 직접 손으로 계산하다 실수를
  한 번 발견해서 python3/node로 실제 실행해 재검증함 — 자격증처럼 "출력 예측형" 문제가
  많은 콘텐츠는 반드시 실행 검증이 필요하다는 교훈
- 서브에이전트 1개로 오답 길이 균형(정답이 가장 긴 비율 71% → 20%)과 3문제뿐이던 레슨
  12개를 4문제로 보강. 정답 위치는 18/18/18/17로 균등. 구조 검증/tsc/eslint/vitest/
  번들 확인 전부 통과
- **사용자 검토 권장**: 공식 출제기준 원문 없이 검색 정보로 스코프를 잡았고 실기(작업형
  코딩) 대비는 다루지 않음(퀴즈 앱 특성상 코드 제출형 실기는 지원 안 함) — 실제 응시 전
  기출문제나 Q-net 출제기준으로 한 번 더 대조해보면 좋음

### 정보기기운용기능사 대비 트랙 — 완료
- 웹 검색으로 확인한 필기 4과목(전자계산기일반/정보기기일반/정보통신일반/정보통신업무규약,
  각 약 15문항씩 총 60문항)을 근거로 8스테이지 16레슨 64문제 작성:
  진법변환·보수 → 논리회로(게이트/불대수/간소화, 프로그래밍기능사 트랙엔 없던 내용) →
  컴퓨터 하드웨어(CPU/메모리/포트 규격) → PC 장애진단·최신 IT 동향 → OSI 7계층·IP 주소
  클래스 → 라우팅(RIP/OSPF)·네트워크 장비 → 국제·국내 표준화기구(ISO/ITU/IEEE/KS/TTA/W3C) →
  정보통신 법·제도(정보통신망법/개인정보보호법/저작권/정보통신윤리)
- **4과목 중 "정보통신업무규약"(법규·표준화기구)은 공식 조항·최신 개정 내용을 확인할 방법이
  없어서, 처음부터 일반 개념 수준으로만 스코프를 제한**(예: "이 법의 목적은?", "이 기구가
  하는 일은?")하고 구체적인 조항 번호·벌칙·개정일자는 전혀 다루지 않음. 서브에이전트에게
  보강 요청할 때도 이 원칙을 명시해서 새로 추가된 문제도 일반론 수준 유지 확인함
- 서브에이전트 1개로 오답 길이 균형(정답이 가장 긴 비율 75% → 17%)과 3문제뿐이던 레슨
  8개를 4문제로 보강. 정답 위치는 16/16/16/15로 균등. 구조 검증/tsc/eslint/vitest/번들
  확인 전부 통과
- **사용자 검토 권장**: 프로그래밍기능사 트랙과 마찬가지로 공식 출제기준 원문 없이 검색
  정보로 스코프를 잡았고, 실기(작업형 — 네트워크 운용 기초 실무)는 다루지 않음
- 남은 것: 사무자동화산업기사, 정보처리기사(기존 ipe 자리) — **사용자 판단으로 보류**.
  둘 다 이 퀴즈 앱 형식과 안 맞는 영역이 커서(사무자동화산업기사는 엑셀/액세스 실기 위주,
  정보처리기사는 SW설계·구축관리 등 범위가 넓고 전부터 "임의로 시험 범위를 판단해서
  채우지 않기로" 해둔 트랙) 나중에 참고 자료가 생기면 재개하기로 함

## 7. 전체 트랙 최종 검토 + 레슨당 문제 수 보강 — 완료

언어별 트랙 9개(java/python/html/css/flask/ai/js/react/spring/sqld, ai-coding 제외)가 레슨당
1~3문제로 얇았던 걸 사실 검토와 함께 보강. 서브에이전트 8묶음(트랙 2개씩 병렬, 세션 사용량
한도 때문에 4개씩 두 번에 나눠 진행)으로 처리하고 전부 직접 재검증함.

- **사실 검토**: 총 1050여 개 기존 문제를 다시 읽어서 오류·모호함 점검. 실제로 고친 건 대부분
  `language` 라벨 오분류(YAML을 Bash로, HTML/Properties를 Java로 표시한 것) 수준이었고,
  spring-9-1-q2는 코드 예시 숫자가 "기본값을 물어보는 문제"와 모순돼서 기본값(1MB/10MB)으로
  정정. 그 외 정답 자체가 틀린 문제는 발견되지 않음(이전 트랙 작성 때 이미 꼼꼼히 썼다는 뜻)
- **보강**: sqld는 레슨당 5문제, 나머지는 4문제 이상으로 맞춤. 총 1650문제로 증가
  (java 280, python 152, sqld 111, js 136, spring 112, react 100, css 84, ai 80, docker 77,
  html 56, flask 56 — ai-coding 406, ipe 0 제외)
- **검증**: id 중복/정답 인덱스/order 순열/보기 중복 등 구조 오류 0건(1650문제 전수),
  tsc/eslint/vitest(35개)/번들 로드 통과. 정답이 가장 긴 비율은 전체 26%(무작위 기대값 25%에
  근접) — 이번엔 각 에이전트가 작성 단계에서부터 길이 균형을 맞춰서 별도 후속 보정이 필요 없었음
- **에이전트들이 공통으로 짚은, 새 레슨이 필요할 만한 빈 자리**(레슨을 새로 만들지는 않음):
  - 자바: 컬렉션 Iterator/유틸리티, 단위테스트(JUnit) 기초
  - 파이썬: (특별한 빈틈 없음)
  - 자바스크립트: this 바인딩, 프로토타입/클래스 상속, ES 모듈, Map/Set, 이벤트 루프
  - 리액트: 커스텀 훅, Suspense/lazy, 에러 바운더리, 테스트(RTL)
  - 스프링: JPA 연관관계(@OneToMany 등), Bean Validation 심화, 로깅, Actuator, 테스트
  - 도커: 네트워크 직접 정의, ENTRYPOINT vs CMD, 멀티스테이지 빌드, HEALTHCHECK
  - SQLD: 트랜잭션 격리수준, DCL(WITH GRANT OPTION 등) — 지금도 시험 커버리지엔 충분하다고 판단
- **작업 중 알게 된 것**: 서브에이전트를 한 번에 8개 병렬로 띄우면 세션 사용량 한도(429, 고정
  시각 초기화)에 걸릴 수 있음 — 작은 테스트 에이전트 하나가 성공한 걸로 봐서 완전 소진이 아니라
  순간적으로 몰려서 걸린 것으로 추정. 4개씩 나눠 순차로 돌리니 문제없이 끝남. 앞으로 대규모
  서브에이전트 배치는 4개 이하로 나눠 진행할 것

## 이미 완료된 것 (추가추가.md 항목 중)
- **UI 테마**: 터미널 컨셉 테마 10종(다크 5 + 라이트 5) 이미 구현됨 (`shared/theme/themes.ts`),
  cmd 창 느낌의 평면/미니멀 스타일도 이미 적용됨 — 원하던 "6가지 정도"는 초과 달성
- **오답 이유 설명(그 순간)**: 문제 풀 때 틀리면 `FeedbackBanner`에 explanation이 바로 뜨는 기능은
  이미 있음. 1번 오답노트에서는 이걸 나중에도 다시 볼 수 있게 만드는 것
