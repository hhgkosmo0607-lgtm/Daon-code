[
  {
    "id": "react-3-1-q1",
    "lessonId": "react-3-1",
    "type": "choice",
    "prompt": "아래처럼 괄호를 붙여 핸들러를 넘기면?",
    "code": "<button onClick={handleClick()}>확인</button>",
    "options": [
      "클릭할 때마다 handleClick이 정상적으로 실행된다",
      "괄호 때문에 SyntaxError가 발생해서 빌드가 실패한다",
      "렌더링될 때 바로 실행되고 클릭에는 반응이 없다",
      "클릭할 때 이벤트가 겹쳐서 두 번 실행된다"
    ],
    "answer": 2,
    "explanation": "handleClick()은 호출 결과를 넘기는 거예요. 함수 자체를 넘기려면 onClick={handleClick}, 인자가 필요하면 onClick={() => handleClick(1)}로 써요."
  },
  {
    "id": "react-3-1-q2",
    "lessonId": "react-3-1",
    "type": "choice",
    "prompt": "JSX에서 클릭 이벤트를 처리하는 올바른 형태는?",
    "options": [
      "<button onclick=\"handleClick()\">",
      "<button on-click={handleClick}>",
      "<button @click={handleClick}>",
      "<button onClick={handleClick}>"
    ],
    "answer": 3,
    "explanation": "리액트 이벤트는 camelCase(onClick)이고 값으로 문자열이 아니라 함수를 넘겨요. @click은 Vue의 문법이에요."
  },
  {
    "id": "react-3-1-q3",
    "lessonId": "react-3-1",
    "type": "choice",
    "prompt": "목록의 각 항목에서 삭제 버튼을 눌렀을 때 그 항목의 id를 넘겨 삭제하는 올바른 코드는?",
    "options": [
      "onClick={() => remove(item.id)}",
      "onClick={remove(item.id)}",
      "onClick={function remove(item.id)}",
      "onClick={\"remove(item.id)\"}"
    ],
    "answer": 0,
    "explanation": "인자가 필요할 땐 화살표 함수로 한 번 감싸서 \"클릭됐을 때 실행할 함수\"를 넘겨야 해요. 감싸지 않으면 렌더링 때 바로 실행돼요."
  },
  {
    "id": "react-3-1-q4",
    "lessonId": "react-3-1",
    "type": "choice",
    "prompt": "부모와 자식에 각각 onClick이 있을 때, 자식 클릭 시 부모의 onClick까지 함께 실행되는 걸 막으려면?",
    "code": "<div onClick={onParentClick}>\n  <button onClick={(e) => { e.stopPropagation(); onChildClick(); }}>클릭</button>\n</div>",
    "options": [
      "e.preventDefault()를 호출해서 이벤트를 막는다",
      "부모의 onClick 핸들러를 지우고 렌더링될 때마다 새로 다시 등록해 주어야 한다",
      "e.stopPropagation()을 호출해서 이벤트 전파를 막는다",
      "자식 버튼을 <a> 태그로 바꿔서 전파 경로를 끊는다"
    ],
    "answer": 2,
    "explanation": "클릭 이벤트는 자식에서 부모로 전파(버블링)돼요. e.stopPropagation()을 호출하면 그 전파를 막아서 부모의 핸들러가 실행되지 않아요."
  }
]
