[
  {
    "id": "spring-13-2-q1",
    "lessonId": "spring-13-2",
    "type": "choice",
    "prompt": "@Valid와 @NotBlank를 함께 쓰는 목적은?",
    "code": "public class JoinRequest {\n    @NotBlank(message = \"아이디는 필수입니다\")\n    private String id;\n}\n\npublic String join(@Valid @RequestBody JoinRequest req) { ... }",
    "language": "Java",
    "options": [
      "요청 데이터가 서비스 로직에 들어오기 전에 형식을 검증하려고",
      "요청마다 필요한 SQL을 검증 결과에 맞춰 자동으로 만들려고",
      "검증 결과를 캐시해 반복 요청의 응답 속도를 높이려고",
      "검증에 통과한 요청에서 트랜잭션을 자동으로 시작하려고"
    ],
    "answer": 0,
    "explanation": "검증에 실패하면 예외가 발생해서 잘못된 입력을 초기에 걸러낼 수 있어요. 클라이언트 검증은 우회 가능하므로 서버 검증이 반드시 필요해요."
  },
  {
    "id": "spring-13-2-q2",
    "lessonId": "spring-13-2",
    "type": "choice",
    "prompt": "@RestControllerAdvice와 @ExceptionHandler를 사용하는 이유는?",
    "code": "@RestControllerAdvice\npublic class GlobalExceptionHandler {\n    @ExceptionHandler(NoSuchElementException.class)\n    public ResponseEntity<String> notFound(Exception e) {\n        return ResponseEntity.status(404).body(\"데이터가 없습니다\");\n    }\n}",
    "language": "Java",
    "options": [
      "예외가 발생하지 않도록 사전에 모든 입력을 차단하려고",
      "try-catch 반복 없이 예외를 한곳에서 일관된 응답으로 처리하려고",
      "예외 메시지를 콘솔이 아닌 파일에만 기록되도록 바꾸려고",
      "발생한 모든 예외를 삼켜서 항상 200 OK로 응답하려고"
    ],
    "answer": 1,
    "explanation": "예외 종류별로 알맞은 상태 코드와 오류 본문을 정해 두면 API 응답이 일관되고 코드가 깔끔해져요."
  },
  {
    "id": "spring-13-2-q3",
    "lessonId": "spring-13-2",
    "type": "choice",
    "prompt": "@Valid로 검증한 매개변수 바로 다음에 BindingResult를 추가하면 어떻게 달라질까?",
    "code": "public String join(@Valid JoinRequest req, BindingResult bindingResult) {\n    if (bindingResult.hasErrors()) {\n        return \"join\";\n    }\n    ...\n}",
    "language": "Java",
    "options": [
      "BindingResult 매개변수만 함께 선언해 두면 검증 자체를 아예 건너뛰고 무조건 통과시켜 버린다",
      "검증 실패 시 서버가 즉시 500 오류로 응답을 강제한다",
      "BindingResult는 검증 결과와 무관하게 항상 비어 있는 객체다",
      "검증 실패해도 예외를 던지지 않고 오류 정보를 BindingResult에 담아 직접 처리할 수 있다"
    ],
    "answer": 3,
    "explanation": "BindingResult가 없으면 검증 실패 시 MethodArgumentNotValidException이 던져지지만, 바로 다음 매개변수로 받아 두면 예외 대신 오류 정보를 직접 꺼내 화면 처리를 이어갈 수 있어요."
  },
  {
    "id": "spring-13-2-q4",
    "lessonId": "spring-13-2",
    "type": "choice",
    "prompt": "회원가입 나이 필드가 0보다 작은 값으로 들어오지 못하게 검증하려면 어떤 어노테이션이 알맞을까?",
    "code": "@Min(0)\nprivate int age;",
    "language": "Java",
    "options": [
      "@Min(0) (지정한 값 이상인지 검사한다)",
      "@NotBlank (문자열이 비어 있지 않은지 검사한다)",
      "@Email (이메일 형식인지 검사한다)",
      "@Past (과거 날짜인지 검사한다)"
    ],
    "answer": 0,
    "explanation": "@Min(0)은 숫자 필드가 지정한 값 이상인지 검사해요. @NotBlank는 문자열 전용이라 int 필드에는 적용할 수 없어요."
  }
]
